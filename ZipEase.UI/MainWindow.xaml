<ui:FluentWindow x:Class="ZipEase.UI.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:core="clr-namespace:ZipEase.UI.Core"
                 Title="ZipEase" Height="600" Width="900"
                 WindowBackdropType="Mica" ExtendsContentIntoTitleBar="True"
                 WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ui:TitleBar Grid.Row="0" Title="ZipEase" ShowMinimize="True" ShowMaximize="False" ShowClose="True"/>
        <Grid Grid.Row="1" AllowDrop="True" DragEnter="OnDragEnter" DragOver="OnDragOver" DragLeave="OnDragLeave" Drop="OnDrop">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="{DynamicResource ApplicationBackgroundBrush}" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="0,0,1,0">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Margin="8,12,8,0">
                        <Border x:Name="ExtractNavItem" CornerRadius="6" Padding="10,8" Margin="0,0,0,2" Cursor="Hand" MouseLeftButtonDown="OnExtractTabClick">
                            <Border.Style><Style TargetType="Border"><Setter Property="Background" Value="Transparent"/><Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}"/></Trigger></Style.Triggers></Style></Border.Style>
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon x:Name="ExtractNavIcon" Symbol="FolderArrowUp24" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="ExtractNavText" Text="{Binding Source={x:Static core:L.Current}, Path=Nav_Extract}" FontSize="13" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <Border x:Name="CompressNavItem" CornerRadius="6" Padding="10,8" Margin="0,0,0,2" Cursor="Hand" MouseLeftButtonDown="OnCompressTabClick">
                            <Border.Style><Style TargetType="Border"><Setter Property="Background" Value="Transparent"/><Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}"/></Trigger></Style.Triggers></Style></Border.Style>
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon x:Name="CompressNavIcon" Symbol="Archive24" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="CompressNavText" Text="{Binding Source={x:Static core:L.Current}, Path=Nav_Compress}" FontSize="13" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    <StackPanel DockPanel.Dock="Bottom" Margin="8,0,8,12">
                        <Border x:Name="SettingsNavItem" CornerRadius="6" Padding="10,8" Cursor="Hand" MouseLeftButtonDown="OnSettingsTabClick">
                            <Border.Style><Style TargetType="Border"><Setter Property="Background" Value="Transparent"/><Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}"/></Trigger></Style.Triggers></Style></Border.Style>
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon x:Name="SettingsNavIcon" Symbol="Settings24" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="SettingsNavText" Text="{Binding Source={x:Static core:L.Current}, Path=Nav_Settings}" FontSize="13" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </DockPanel>
            </Border>
            <Grid Grid.Column="1">
                <Grid x:Name="ExtractPanel" AllowDrop="True">
                    <Border Visibility="{Binding IsIdleVisible, Converter={StaticResource BoolToVisibility}}" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="2" CornerRadius="12" Margin="40" Cursor="Hand" MouseLeftButtonDown="OnDropZoneClick">
                        <Border.Style><Style TargetType="Border"><Style.Triggers><DataTrigger Binding="{Binding IsDragOverActive}" Value="True"><Setter Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}"/><Setter Property="Background" Value="{DynamicResource SystemAccentColorSecondaryBrush}"/></DataTrigger></Style.Triggers></Style></Border.Style>
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <ui:SymbolIcon Symbol="Archive24" FontSize="72" HorizontalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{Binding Source={x:Static core:L.Current}, Path=DropZone_Title}" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,24,0,0"/>
                            <TextBlock Text="{Binding Source={x:Static core:L.Current}, Path=DropZone_Subtitle}" FontSize="13" HorizontalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                    <Grid Visibility="{Binding IsPreviewVisible, Converter={StaticResource BoolToVisibility}}" Margin="20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,8,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ui:Button Grid.Column="0" Icon="ArrowLeft24" Command="{Binding NavigateBackCommand}" Visibility="{Binding IsBackButtonVisible, Converter={StaticResource BoolToVisibility}}" Margin="0,0,8,0" ToolTip="{Binding Source={x:Static core:L.Current}, Path=Extract_Back_Tooltip}"/>
                            <TextBlock Grid.Column="1" Text="{Binding LoadedArchivePath}" FontSize="13" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Grid.Column="2" Text="{Binding ArchiveSummary}" FontSize="12" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="12,0,12,0"/>
                            <ui:Button Grid.Column="3" x:Name="ExtractButton" Content="{Binding Source={x:Static core:L.Current}, Path=Extract_ExtractAll}" Icon="FolderArrowUp24" Click="OnExtractClick" IsEnabled="{Binding IsExtractButtonEnabled}" Appearance="Primary" Margin="0,0,8,0"/>
                            <ui:Button Grid.Column="4" Content="{Binding Source={x:Static core:L.Current}, Path=Extract_Close}" Icon="Dismiss24" Command="{Binding ResetCommand}"/>
                        </Grid>

                        <ui:TextBox Grid.Row="1" x:Name="SearchBox" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="{Binding Source={x:Static core:L.Current}, Path=Extract_SearchPlaceholder}" Icon="Search24" ClearButtonEnabled="True" Margin="0,0,0,8" KeyDown="OnSearchBoxKeyDown"/>
                        <DataGrid Grid.Row="2" x:Name="ArchiveDataGrid" ItemsSource="{Binding ArchiveEntries}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Extended" GridLinesVisibility="Horizontal" HeadersVisibility="Column" FontSize="13" RowHeight="32" BorderThickness="0" CanUserSortColumns="True" MouseDoubleClick="OnDataGridDoubleClick" PreviewMouseLeftButtonDown="OnDataGridPreviewMouseLeftButtonDown" PreviewMouseMove="OnDataGridPreviewMouseMove">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="{Binding Source={x:Static core:L.Current}, Path=Column_FileName}" Binding="{Binding FileName}" SortMemberPath="FileName" Width="*"/>
                                <DataGridTextColumn Header="{Binding Source={x:Static core:L.Current}, Path=Column_Type}" Binding="{Binding FileType}" SortMemberPath="FileType" Width="80"/>
                                <DataGridTextColumn Header="{Binding Source={x:Static core:L.Current}, Path=Column_Size}" Binding="{Binding FormattedSize}" SortMemberPath="SizeBytes" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <StackPanel Grid.Row="3" Visibility="{Binding IsProgressVisible, Converter={StaticResource BoolToVisibility}}" Margin="0,12,0,0">
                            <TextBlock Text="{Binding CurrentExtractionFile}" FontSize="12" Margin="0,0,0,8" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ProgressBar Grid.Column="0" Value="{Binding ExtractionProgress}" Height="6" Minimum="0" Maximum="100"/>
                                <TextBlock Grid.Column="1" Text="{Binding ExtractionProgress, StringFormat='{}{0}%'}" FontSize="11" Margin="8,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                <ui:Button Grid.Column="2" Content="取消" Icon="Dismiss24" Command="{Binding CancelBatchCommand}" Visibility="{Binding IsCancelBatchVisible, Converter={StaticResource BoolToVisibility}}" Appearance="Secondary" Margin="8,0,0,0" VerticalAlignment="Center"/>
                            </Grid>
                        </StackPanel>
                        <StackPanel Grid.Row="4" Margin="0,8,0,0">
                            <ui:InfoBar IsOpen="{Binding IsStatusVisible, Mode=TwoWay}" Message="{Binding StatusMessage}" IsClosable="True"/>
                            <ui:Button Content="{Binding Source={x:Static core:L.Current}, Path=Extract_TrashButton}" Command="{Binding TrashSourceCommand}" IsEnabled="{Binding IsTrashButtonEnabled}" Visibility="{Binding IsTrashButtonEnabled, Converter={StaticResource BoolToVisibility}}" Appearance="Secondary" Margin="0,6,0,0" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Grid>
                </Grid>
                <core:CompressView x:Name="CompressPanel" Visibility="Collapsed" AllowDrop="True" DragEnter="OnCompressDragEnter" DragOver="OnCompressDragOver" Drop="OnCompressDrop"/>
                <core:SettingsView x:Name="SettingsPanel" Visibility="Collapsed"/>
                <!-- Image Preview Panel (overlay, initially collapsed) -->
                <core:PreviewPanel x:Name="ImagePreviewPanel" Visibility="Collapsed"/>
            </Grid>
        </Grid>
    </Grid>
</ui:FluentWindow>